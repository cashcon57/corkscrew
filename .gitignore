# Rust
target/

# Node
node_modules/
/build
/.svelte-kit
/package

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# MCP (contains absolute paths)
.mcp.json

# Project data
*.db
*.sqlite

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# OAuth client secrets (credentials embedded in binary, JSON stays local)
client_secret_*.json

# Signing keys (only public keys are committed)
.keys/*
!.keys/*.pub

# Release artifacts
latest.json
*.sig

# Screenshots (dev/testing)
Screenshots/
crashlogs-page.png
dashboard-initial.png
discover-page.png
mods-no-game.png
phase1-*.png
phase2-*.png
settings-dark-mode.png
settings-page.png
avatar.jpg
noun-arrow-right-*.png
"DMG Arrow.svg"

# Raw icon sources (processed into static/icons/)
tools:sites icons/

# DepotDownloader cache (game version downloads)
depots/

# Playwright MCP
.playwright-mcp/

# Brand kit source (too large for git)
brand-kit/source-highres.png
brand-kit/*.xcf

# Screenshots and media (not needed in repo)
Screenshots/
*.xcf
*.mov
playwright-report/
test-results/
DMG Arrow.svg
corkscrew-state.png
