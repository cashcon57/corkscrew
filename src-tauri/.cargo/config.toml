# Mold linker is enabled via CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_LINKER
# and CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_RUSTFLAGS env vars in CI,
# not here — a hard config.toml breaks builds where mold isn't installed.
